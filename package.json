{
  "name": "awstest",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "build": "npx esbuild src/index.ts --bundle --platform=node --outdir=dist/index.js",
    "dev": "npm run build && node dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@types/express": "^5.0.0",
    "esbuild": "^0.24.0",
    "express": "^4.21.1"
  }
}
